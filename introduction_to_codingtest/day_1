# 문제 1.
"""
정수 num1과 num2가 매개변수로 주어집니다. 
두 수가 같으면 1 다르면 -1을 retrun하도록 solution 함수를 완성해주세요.
"""


def solution(num1, num2):
    return 1 if num1 == num2 else -1


# 한줄 if 문
# 복잡한 if문의 경우 여러 줄로 표현하는 것이 가독성이 좋을 수 있으나,
# 매우 단순한 경우 한 줄로 표현한다면 코드도 줄이고 간결하게 사용할 수 있다.

# if - else문
# A if 조건 else B
# 조건 값이 True면 A, False면 B를 반환

# if - elif - else 문
# A if 조건1 else B if 조건2 else C
# 조건1 값이 True이면 A, 조건2 값이 True이면 B, 두 조건 모두 False이면 C 반환
